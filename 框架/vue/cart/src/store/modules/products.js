const state = {
}

const getters = {
}

const actions = {
}

const mutations = {
}

// state.cart.state 
export default {
    namespaced: true, // 支持命名空间 
    state,
    getters,
    actions,
    mutations
}